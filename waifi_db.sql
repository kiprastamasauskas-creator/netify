USE waifi_db;

-- 1. Add the boolean flag to track verification status
-- ALTER TABLE users 
-- ADD COLUMN is_verified BOOLEAN DEFAULT FALSE;

-- 2. Add the column to store the temporary 6-digit code
-- ALTER TABLE users 
-- ADD COLUMN registration_otp VARCHAR(6);
